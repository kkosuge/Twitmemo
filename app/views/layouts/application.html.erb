<!DOCTYPE html>
<html>
  <head>
    <title>Twitmemo</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application" %>
    <script type="text/javascript" src="http://www.zurb.com/javascripts/plugins/jquery.textchange.min.js"></script>
    <style type="text/css">
      body {
        padding-top: 40px;

        background:url(wood2.jpg);
      }
      .content {
        -webkit-box-shadow: 0 0px 0px rgba(0,0,0,.0);
        -moz-box-shadow: 0 0px 0px rgba(0,0,0,.0);
        box-shadow: 0 0px 0px rgba(0,0,0,.0);
        border-top-left-radius: 6px;
        border-top-right-radius: 6px;
        margin-top: 20px;
      }
      footer p {
        text-align: center;
      }
      .container {
        width: 820px;
      }
      h2 {
        margin: -12px 0px -5px 0px;
      }
      h4 {
        margin: 0px 0px -5px 0px; 
      }
      #list ul { list-style: none; }
      #list ul li a { text-decoration:none; color:#000000; line-height:20px; }
      #list ul li a:hover { color:#ffa500; }
      #list  { margin-left:-20px; }  
      .label a, a:hover { color:#fff;text-decoration:none; }
      .top a {color:#000}
      a:hover{color:#ffa500}
    </style>
  </head>
  <body id="top">
    <div class = "topbar"> 
      <%= render 'layouts/topbar' %>
    </div>
    <div class="container">
      <div class="content">
        <% if current_user -%>
          <div class="page-header headerarea"></div> 
          <%= yield %>
          <%= render 'layouts/sidebar' %>
          <%= render 'layouts/popup' %>
        <% else %>
          <%= yield %>
        <% end -%>
      </div>
      <footer>
        <p>&copy; Twitmemo 2012</p>
      </footer>
    </div>
  </body>
</html>
